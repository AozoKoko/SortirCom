{% extends 'base.html.twig' %}

{% block body %}
    test

  {{ form_start(Form) }}
  {{ form_widget(Form) }}
    <button type="btn btn-primary">Submit</button>
  {{ form_end(Form) }}


{% endblock %}